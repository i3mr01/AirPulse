<Window x:Class="AirPulse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AirPulse Server" Height="680" Width="400"
        Icon="pack://application:,,,/Resources/AirPulse.ico"
        WindowStyle="None" ResizeMode="NoResize" AllowsTransparency="True"
        Background="Transparent"
        MouseDown="Window_MouseDown">
    
    <Border CornerRadius="20" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,20,0,20">
                <TextBlock Text="AirPulse" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Remote Control Server" FontSize="14" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
            </StackPanel>
            <!-- Main Content -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                
                <!-- QR Code -->
                <Border CornerRadius="15" Background="White" Padding="10" Margin="0,0,0,20" HorizontalAlignment="Center">
                    <Image x:Name="QrCodeImage" Width="200" Height="200" RenderOptions.BitmapScalingMode="NearestNeighbor" SnapsToDevicePixels="True"/>
                </Border>

                <!-- PIN -->
                <TextBlock Text="PAIRING PIN" FontSize="12" FontWeight="Bold" Foreground="#888888" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock x:Name="PinText" Text="000 000" FontSize="36" FontWeight="Bold" Foreground="#00D1FF" HorizontalAlignment="Center"/>

                <!-- Connection Info -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,5">
                    <TextBlock Text="Connect at: " Foreground="#AAAAAA" FontSize="14"/>
                    <TextBlock x:Name="IpText" Text="http://192.168.1.X:5000" Foreground="White" FontWeight="Bold" FontSize="14" Cursor="Hand" MouseDown="IpText_MouseDown" ToolTip="Click to cycle IPs"/>
                </StackPanel>
                
                <Button Content="Fix Connection" Click="FixConnection_Click" Background="Transparent" Foreground="#FF5555" BorderThickness="0" Cursor="Hand" FontSize="12" Margin="0,0,0,20" HorizontalAlignment="Center">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <TextBlock Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" TextDecorations="Underline"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                 
                 <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                     <TextBlock Text="Start with Windows" Foreground="#AAAAAA" VerticalAlignment="Center" Margin="0,0,10,0"/>
                     <ToggleButton x:Name="AutoStartToggle" Width="60" Height="40" Checked="AutoStartCheckBox_Checked" Unchecked="AutoStartCheckBox_Unchecked" Cursor="Hand" Background="Transparent">
                         <ToggleButton.Template>
                             <ControlTemplate TargetType="ToggleButton">
                                 <Grid Background="Transparent">
                                     <!-- Track Container -->
                                     <Grid Width="50" Height="28" VerticalAlignment="Center" HorizontalAlignment="Center">
                                         <Border x:Name="Track" Background="#3A3A3A" CornerRadius="14" BorderBrush="#555555" BorderThickness="1"/>
                                         <Grid x:Name="Thumb" HorizontalAlignment="Left" Width="24" Height="24" Margin="2,0,0,0">
                                             <Ellipse Fill="White">
                                                 <Ellipse.Effect>
                                                     <DropShadowEffect ShadowDepth="2" BlurRadius="4" Opacity="0.2" Direction="270"/>
                                                 </Ellipse.Effect>
                                             </Ellipse>
                                         </Grid>
                                     </Grid>
                                 </Grid>
                                 <ControlTemplate.Triggers>
                                     <Trigger Property="IsChecked" Value="True">
                                         <Setter TargetName="Track" Property="Background" Value="#00D1FF"/>
                                         <Setter TargetName="Track" Property="BorderBrush" Value="#00D1FF"/>
                                         <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                         <Setter TargetName="Thumb" Property="Margin" Value="0,0,2,0"/>
                                     </Trigger>
                                 </ControlTemplate.Triggers>
                             </ControlTemplate>
                         </ToggleButton.Template>
                     </ToggleButton>
                 </StackPanel>

            </StackPanel>

            <!-- Footer -->
            <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,20,0,10">
                <TextBlock x:Name="StatusText" Text="Waiting for connections..." FontSize="12" Foreground="#666666"/>
            </StackPanel>
            
            <!-- Close Button -->
            <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                    Background="Transparent" Foreground="White" BorderThickness="0"
                    Click="CloseButton_Click" Margin="0,-10,-10,0" FontSize="16" Cursor="Hand"/>
        </Grid>
    </Border>
</Window>
